import React from 'react';

const OrderComplete = () => {
    return (
        <div>
            <h1 className="text-center mt-5 pt-5">Order Completed</h1>
        </div>
    );
};

export default OrderComplete;
